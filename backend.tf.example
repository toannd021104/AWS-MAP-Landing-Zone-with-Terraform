# =============================================================================
# AWS MAP Landing Zone - Backend Configuration
# =============================================================================
# Uncomment and configure the backend block for remote state management
# Using S3 native locking (Terraform 1.10+) - no DynamoDB required!
# =============================================================================

terraform {
  backend "s3" {
    bucket       = "your-terraform-state-bucket-YOUR_ACCOUNT_ID"  # Replace with your S3 bucket
    key          = "aws-map-landing-zone/terraform.tfstate"
    region       = "ap-southeast-1"  # Change to your region
    encrypt      = true
    use_lockfile = true  # S3 native state locking (Terraform 1.10+)
  }
}

# =============================================================================
# Benefits of S3 native locking (use_lockfile = true):
# - No DynamoDB table required
# - Reduced infrastructure overhead
# - Simpler configuration
# - Lower cost (no DynamoDB charges)
#
# Note: Requires Terraform >= 1.10.0
# =============================================================================
